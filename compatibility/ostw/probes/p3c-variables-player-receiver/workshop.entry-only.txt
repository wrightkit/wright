variables
{
    global:
        0: sink
    player:
        0: p
}

// Rule Element Count: 3
rule("Initial Global")
{
    event
    {
        Ongoing - Global;
    }

    // Element Count: 2, Action Count: 1
    actions
    {
        Set Global Variable(sink, 0);
    }
}

// Rule Element Count: 12
rule("P3c player-variable receiver read and write")
{
    event
    {
        Ongoing - Each Player;
        All;
        All;
    }

    // Element Count: 11, Action Count: 4
    // Largest Action Index: 3 using 4 Elements
    actions
    {
        Set Player Variable(Event Player, p, 5);
        Modify Player Variable(Event Player, p, Add, 2);
        Set Global Variable(sink, Player Variable(Event Player, p));
        Big Message(Event Player, Custom String("{0}", Global Variable(sink)));
    }
}
