globalvar Number sink = 0;
playervar Number p;

rule: "P3c player-variable receiver read and write"
Event.OngoingPlayer
{
    EventPlayer().p = 5;
    EventPlayer().p += 2;
    sink = EventPlayer().p;
    BigMessage(EventPlayer(), <"<0>", sink>);
}
