variables
{
    global:
        0: a
        1: b
        127: i
        2: c
        128: d
        3: e
}

// Rule Element Count: 9
rule("Initial Global")
{
    event
    {
        Ongoing - Global;
    }

    // Element Count: 8, Action Count: 4
    // Largest Action Index: 0 using 2 Elements
    actions
    {
        Set Global Variable(a, 1);
        Set Global Variable(b, 2);
        Set Global Variable(c, 3);
        Set Global Variable(e, 4);
    }
}

// Rule Element Count: 21
rule("P3a use all variables")
{
    event
    {
        Ongoing - Global;
    }

    // Element Count: 20, Action Count: 2
    // Largest Action Index: 0 using 14 Elements
    actions
    {
        Modify Global Variable(i, Add, Add(Add(Add(Add(Global Variable(a), Global Variable(b)), Global Variable(c)), Global Variable(d)), Global Variable(e)));
        Big Message(All Players(All Teams), Custom String("{0}", Global Variable(i)));
    }
}
