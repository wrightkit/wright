// elementCount: 30
// ---- workshopCode ----
variables
{
    global:
        0: e
}

// Rule Element Count: 3
rule("Initial Global")
{
    event
    {
        Ongoing - Global;
    }

    // Element Count: 2, Action Count: 1
    actions
    {
        Set Global Variable(e, 0);
    }
}

// Rule Element Count: 27
rule("r")
{
    event
    {
        Ongoing - Each Player;
        All;
        All;
    }

    // Element Count: 26, Action Count: 7
    // Largest Action Index: 2 using 10 Elements
    actions
    {
        Create Effect(All Players(All Teams), Orb, Color(Black), Subtract(Left, Left), 0, Visible To Position and Radius);
        Set Global Variable(e, Last Created Entity);
        Create Progress Bar In-World Text(All Players(All Teams), 50, Custom String("t"), Subtract(Left, Left), 1, Do Not Clip, Color(White), Color(White), Visible To And Values, Default Visibility);
        Set Global Variable(e, Last Text ID);
        Play Effect(Event Player, Buff Impact Sound, Color(White), Subtract(Left, Left), 0);
        Disable Movement Collision With Environment(Event Player, False);
        Enable Movement Collision With Environment(Event Player);
    }
}
