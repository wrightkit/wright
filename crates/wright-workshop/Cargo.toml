[package]
name = "wright-workshop"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Wright's native Workshop language model: canonical catalog, lexer/parser, and emitter."

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
wright-ir = { path = "../wright-ir" }

[dev-dependencies]
regex = "1"
