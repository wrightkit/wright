[package]
name = "wright-workshop"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Cutover adapter for the workshop-rs canonical Workshop core (wright#143); re-exports only, no implementation."

[lints]
workspace = true

[dependencies]
# Single pinned reference: `[workspace.dependencies]` in the root Cargo.toml.
workshop-rs.workspace = true

[dev-dependencies]
regex = "1"
serde_json = "1"
wright-core = { path = "../wright-core" }
wright-ir = { path = "../wright-ir" }
wright-opy = { path = "../wright-opy" }
