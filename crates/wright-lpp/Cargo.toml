[package]
name = "wright-lpp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Wright's Language Provider Protocol (LPP v1) client: provider discovery, stdio process lifecycle, JSON-RPC correlation, capability negotiation, and structured failures."

[lints]
workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
os_pipe = "1"
