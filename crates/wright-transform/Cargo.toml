[package]
name = "wright-transform"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Wright's WIR transformation pipeline: optimization profiles and evidence-backed compat passes."

[lints]
workspace = true

[dependencies]
serde = { workspace = true, features = ["derive"] }
wright-ir.workspace = true
# Single pinned reference: `[workspace.dependencies]` in the root Cargo.toml.
workshop-rs.workspace = true
